import React from "react";

import UnstyledButton from "../UnstyledButton";

const Header = () => {
  return (
    <>
      <UnstyledButton> Donald Trump </UnstyledButton>
      <UnstyledButton>Hillary Clinton</UnstyledButton>
    </>
  );
};

export default Header;
